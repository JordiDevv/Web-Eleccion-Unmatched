CREATE TABLE IF NOT EXISTS users
(
    id INTEGER PRIMARY KEY,
    nombre TEXT NOT NULL,
    alicia INTEGER DEFAULT 0,
    rey_arturo INTEGER DEFAULT 0,
    medusa INTEGER DEFAULT 0,
    simbad INTEGER DEFAULT 0,
    dracula INTEGER DEFAULT 0,
    jekyll_hyde INTEGER DEFAULT 0,
    sherlock INTEGER DEFAULT 0,
    invisible INTEGER DEFAULT 0,
    caperucita INTEGER DEFAULT 0,
    beowulf INTEGER DEFAULT 0,
    aquiles INTEGER DEFAULT 0,
    bloody_mary INTEGER DEFAULT 0,
    sun_wukong INTEGER DEFAULT 0,
    yennenga INTEGER DEFAULT 0,
    houdini INTEGER DEFAULT 0,
    genio INTEGER DEFAULT 0
);